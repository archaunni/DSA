class MyQueue(object):

    def __init__(self):
        self.stack_push=[]
        self.stack_pop=[]
    def push(self, x):
        """
        :type x: int
        :rtype: None
        """
        self.stack_push.append(x)

    def pop(self):
        """
        :rtype: int
    
        """
        self.stack_pop=[]
        for i in range(len(self.stack_push)):
            self.stack_pop.append(self.stack_push.pop())
        pop_elmt=self.stack_pop.pop()
        self.stack_push=[]
        for i in range(len(self.stack_pop)):
            self.stack_push.append(self.stack_pop.pop())
        return pop_elmt

    def peek(self):
        """
        :rtype: int
        """
        self.stack_pop=[]
        for i in range(len(self.stack_push)):
            self.stack_pop.append(self.stack_push.pop())
        top_elmt=self.stack_pop[-1]
        self.stack_push=[]
        for i in range(len(self.stack_pop)):
            self.stack_push.append(self.stack_pop.pop())
        return top_elmt

    def empty(self):
        """
        :rtype: bool
        """
        if not self.stack_push:
            return True
        return False
